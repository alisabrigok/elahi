import { defineMessages } from "react-intl";

export const scope = "navigators.HomeNavigator";

export default defineMessages({
  tabBarLabel: {
    id: `${scope}.tabBarLabel`,
    defaultMessage: "Due",
  },
});
