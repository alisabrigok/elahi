import { defineMessages } from "react-intl";

export const scope = "screens.CalendarScreen";

export default defineMessages({
  calendar: {
    id: `${scope}.calendar`,
    defaultMessage: "Calendar",
  },
});
